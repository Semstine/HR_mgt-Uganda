import DscModulePage from '@/components/dsc/DscModulePage'

export default function Page() {
  return <DscModulePage title="Settings / Reference Data" subtitle="Districts, DSCs, departments, salary scales, schemes, job descriptions and person specifications." moduleKey="settings" />
}
